# وثيقة حالات اختبار واجهة برمجة التطبيقات (API Test Cases) — مشروع مهام (Maham)

توضح هذه الوثيقة حالات الاختبار المنهجية المجهزة لواجهات مشروع **مهام**، للتأكد من موثوقية وأمان النظام ومطابقته لجميع القواعد البرمجية والتحقق من المدخلات.

---

## جدول حالات الاختبار (Test Cases Table)

| الرقم | الوصف | المدخلات | الحالة المتوقعة | الحالة الفعلية | النتيجة |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **01** | تسجيل مستخدم جديد صحيح | `{"fullName": "محمد قيس", "email": "test@maham.com", "password": "Password123!"}` | `201 Created` | | |
| **02** | تسجيل دخول صحيح بالبيانات المسجلة | `{"email": "test@maham.com", "password": "Password123!"}` | `200 OK` | | |
| **03** | جلب بيانات المستخدم الحالي (الملف الشخصي) | `GET /api/auth/me` مع استخدام `Token` الصحيح | `200 OK` | | |
| **04** | تسجيل دخول بكلمة مرور خاطئة | `{"email": "test@maham.com", "password": "WrongPass999"}` | `401 Unauthorized` | | |
| **05** | تسجيل مستخدم جديد ببريد إلكتروني مكرر في النظام | `{"fullName": "مستخدم", "email": "test@maham.com", "password": "Password123!"}` | `400 Bad Request` | | |
| **06** | تسجيل مستخدم بكلمة مرور ضعيفة (مخالفة لقواعد التحقق) | `{"fullName": "مستخدم", "email": "weak@test.com", "password": "123"}` | `400 Bad Request` | | |
| **07** | طلب جلب المشاريع بدون توكن (غير مصرح به) | `GET /api/projects` بدون إرسال `Token` | `401 Unauthorized` | | |
| **08** | إنشاء مشروع جديد | `{"name": "مشروع التدريب", "description": "مشروع مهام الميداني"}` مع `Token` | `201 Created` | | |
| **09** | عرض قائمة المشاريع التي ينتمي إليها المستخدم | `GET /api/projects` مع استخدام `Token` | `200 OK` | | |
| **10** | عرض تفاصيل مشروع محدد باستخدام الـ ID | `GET /api/projects/{{project_id}}` مع استخدام `Token` | `200 OK` | | |
| **11** | تحديث بيانات مشروع قائم | `{"name": "مشروع مهام المحدّث", "description": "وصف جديد"}` مع `Token` | `200 OK` | | |
| **12** | محاولة جلب مشروع غير موجود بالـ ID (UUID فارغ) | `GET /api/projects/00000000-0000-0000-0000-000000000000` | `404 Not Found` | | |
| **13** | محاولة حذف مشروع بدون صلاحيات (بدون توكن) | `DELETE /api/projects/{{project_id}}` بدون إرسال `Token` | `401 Unauthorized` | | |
| **14** | إنشاء لوحة كانبان جديدة للمشروع | `{"name": "لوحة التطوير", "description": "لوحة Kanban", "backgroundColor": "#1a1a2e"}` | `201 Created` | | |
| **15** | جلب تفاصيل اللوحة التي تم إنشاؤها بالـ ID | `GET /api/boards/{{board_id}}` | `200 OK` | | |
| **16** | تعديل بيانات لوحة الكانبان (تغيير العنوان واللون) | `{"name": "لوحة التطوير المحدّثة", "backgroundColor": "#0f3460"}` | `200 OK` | | |
| **17** | إنشاء عمود جديد في لوحة الكانبان | `{"name": "لم تبدأ", "order": 1}` | `201 Created` | | |
| **18** | تعديل بيانات عمود (تحديث الاسم ووضع حد أقصى للمهام WIP) | `{"name": "قيد الانتظار", "wipLimit": 5}` | `200 OK` | | |
| **19** | إنشاء بطاقة جديدة في العمود | `{"title": "تصميم شاشة تسجيل الدخول", "description": "تصميم Login بـ Flutter", "priority": "High", "storyPoints": 5}` | `201 Created` | | |
| **20** | محاولة إنشاء بطاقة بدون عنوان (مخالفة لقواعد التحقق) | `{"title": "", "priority": "High"}` | `400 Bad Request` | | |
| **21** | جلب تفاصيل البطاقة التي تم إنشاؤها بالـ ID | `GET /api/cards/{{card_id}}` | `200 OK` | | |
| **22** | تعديل بيانات بطاقة (الاسم، الأولوية، نقاط القصة) | `{"title": "تصميم شاشة Login - محدّث", "priority": "Critical", "storyPoints": 8}` | `200 OK` | | |
| **23** | إضافة تعليق جديد على البطاقة | `{"content": "تم مراجعة التصميم — يحتاج تعديل الألوان"}` | `200 OK` أو `201 Created` | | |
| **24** | جلب إحصائيات عامة للمستخدم (Dashboard) | `GET /api/dashboard` مع استخدام `Token` | `200 OK` | | |
| **25** | جلب سجل النشاطات العام للمستخدم الحالي | `GET /api/activity/me` مع استخدام `Token` | `200 OK` | | |

---

### إرشادات التشغيل للطالب:
1. قم بتشغيل خادم الـ API محلياً للتأكد من استجابته على المنفذ `http://localhost:5233`.
2. افتح تطبيق Postman واستورد ملف البيئة `Maham_Environment.json` وملف المجموعة `Maham_API_Collection.json`.
3. قم بتشغيل المجموعة بالترتيب؛ ستعمل الـ Test Scripts تلقائياً على استخراج المتغيرات (التوكن، المعرفات) وحفظها لتغذية الطلبات التالية.
4. طابق الردود والرموز المستلمة مع "الحالة المتوقعة" في الجدول، ثم قم بملء "الحالة الفعلية" (مثال: `201 Created`) والنتيجة (`ناجح` / `فاشل`).
